import { FilterByConteneurPipe } from './filter-by-conteneur.pipe';

describe('FilterByConteneurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByConteneurPipe();
    expect(pipe).toBeTruthy();
  });
});
